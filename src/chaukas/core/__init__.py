"""Core building blocks shared by every layer.

Domain models, the event bus, clocks, time-indexed data structures and configuration.
Nothing in ``core`` imports from other Chaukas packages, so every layer can depend on it.
"""
